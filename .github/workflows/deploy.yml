name: Deploy
run-name: Deploy conditions test
on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
      - labeled
      - edited
env:
  SOME_VAR: some value
  SITE_ID: 28291f48-2de2-4c95-a222-07e0a5feaa22
  TITLE: gha-fullstack-deploy
jobs:
  deploy-job:
    runs-on: ubuntu-latest
    if: |
      github.event.pull_request.state == 'open' && (
        contains(github.event.pull_request.labels.*.name, 'deploy') ||
        endsWith(github.event.pull_request.title, '[deploy]')
      ) && (
        contains(fromJSON('["opened", "reopened", "synchronize"]'), github.event.action) ||
        github.event.action == 'labeled' && github.event.label.name == 'deploy' ||
        github.event.action == 'edited' && endsWith(github.event.pull_request.title, '[deploy]')
      )
    steps:
      - run: echo "Start"
      - name: GH context
        env:
          GH: ${{ toJson(github)}} ${{ github.event.pull_request.state }}
        run: |
          echo -e "Github context:\n$GH"
      # - name: Fail step
      #   id: fail-step
      #   run: |
      #     asdfasdfasdf
      - uses: actions/github-script@v7
        name: comment after failure
        if: always()
        with:
          script: |
            const status = "${{ job.status }}";
            let body = `Deployment result: ${status}`;
            if (status == "success") {
              body += "\nSome variable: ${{ github.event.pull_request.url }}--${{ env.SOME_VAR }}\nAlso here is empty var value: ${{ env.EMPTY_VAR }}"
            }
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: body
            })
  js-script:
    name: Test JS script step
    runs-on: ubuntu-latest
    needs: deploy-job
    outputs:
      site-url: ${{ steps.site-url.outputs.site-url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - uses: actions/setup-node@v3
        with:
          node-version: "20.x"
      - run: npm install netlify
      - uses: actions/github-script@v7
        name: check netlify status
        id: site-url
        with:
          script: |
            console.log("Importing custom script");
            const { default: netlify_wait } = await import('${{ github.workspace }}/.github/scripts/netlify_wait.js');
            console.log("Imported custom script. Calling...");
            await netlify_wait(core, "${{ env.SITE_ID }}", "${{ env.TITLE }}", "${{ secrets.N_TOKEN }}");
            console.log("Done");

  test-output:
    runs-on: ubuntu-latest
    needs: js-script
    steps:
      - run: |
          echo "OUTPUT: ${{ needs.js-script.outputs.site-url }}"

  js-job:
    name: Test JS Job
    runs-on: ubuntu-latest
    needs: deploy-job
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Test JS Job
        id: js-job
        uses: ./.github/actions/js-action
        with:
          name: tester
          id: ${{ env.SITE_ID }}
          payload: '{ "key": "value", "key2": "value2" }'
          token: ${{ secrets.N_TOKEN }}
          title: ${{ env.TITLE }}
  report-job:
    needs: deploy-job
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-node@v3
        with:
          node-version: "20.x"
      - run: npm install netlify
      - uses: actions/github-script@v7
        name: set check
        with:
          script: |
            var run = true;
            var count = 60;
            async function countdown() {
              console.log(`Countdown tick: ${count}`);
              await new Promise((f) => setTimeout(f, 1000));
              count--;
              if (count <= 0) {
                run = false;
              }
              console.log(`Countdown done: ${count}`);
            }
            async function test() {
              console.log("Start countdown");
              while (run) {
                await countdown();
              }
              console.log("Stop countdown");
            }
            console.log("START");
            await test();
            console.log("DONE");
